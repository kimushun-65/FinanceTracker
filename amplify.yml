version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend
    files:
      - '.next/**/*'
      - 'node_modules/**/*'
      - 'public/**/*'
      - 'package.json'
      - 'package-lock.json'
      - 'next.config.ts'
  cache:
    paths:
      - frontend/node_modules/**/*